# 09-dp.md

## 동적 계획법 (DP)

### 언제 쓰나

- 최적해
- 경우의 수
- 중복되는 부분 문제

### 정의 순서

1. 상태 정의
2. 점화식
3. 초기값
4. 답 도출

### 예시 (피보나치)

```js
function fib(n) {
  const dp = Array(n + 1).fill(0);
  dp[1] = 1;
  for (let i = 2; i <= n; i++) {
    dp[i] = dp[i - 1] + dp[i - 2];
  }
  return dp[n];
}
```
